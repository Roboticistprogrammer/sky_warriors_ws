## Simulation Script

Use this package to start the simulator bootstrap only:

```bash
./startup.sh
```

After that, launch the mission controller from another terminal:

```bash
source install/setup.bash
ros2 launch skyw_control launch_simulation.py
```
